# Evolve Fit Tracker

Monorepo skeleton containing a simple Express server and a Vite + React client.

- server: Express API
- client: Vite + React frontend

Next steps:
- install dependencies in `server` and `client`
- run server: `npm run dev` (server)
- run client: `npm run dev` (client)
